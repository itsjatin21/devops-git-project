# DevOps Git Project

This is a simple DevOps project demonstrating Git workflows.

## Features
- Backup script in Python.
- Dockerfile for containerization.

## Usage
Run `python backup.py` or build the Docker image: `docker build -t backup-app .`

## Branches
- `main`: Production.
- `dev`: Development.
- `feature/*`: Features.
